# Copie este arquivo para .env e configure conforme necessário

# Configuração do Servidor
PORT=3001
HOST=0.0.0.0

# JWT Secret
JWT_SECRET=seu_jwt_secret

# Ambiente
NODE_ENV=development

# URLs permitidas para CORS (separadas por vírgula)
# Use * para permitir todas as origens (apenas em desenvolvimento/LAN)
ALLOWED_ORIGINS=*

# Banco de Dados
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=green_mind

